describe("Data Type", () => {
  "should must declare";
  it("should must declare", () => {
    const name: string = "Hello Dwi";
    const balance: number = 1000;
    const isVip: boolean = true;

    console.info(name, balance, isVip);
  });
});
