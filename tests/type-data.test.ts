describe("Data Type", () => {
  "should must declare";
  it("should must declare", () => {
    let name: string = "Hello Dwi";
    let balance: number = 1000;
    let isVip: boolean = true;

    console.info(name, balance, isVip);
  });
});
